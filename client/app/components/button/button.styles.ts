const baseStyles = 'px-4 py-2 rounded transition duration-300';
const primaryStyles = `bg-primary text-white hover:bg-secondary`;
const secondaryStyles = `bg-secondary text-white hover:bg-primary`;
export const buttonStyles = {
  base: baseStyles,
  primary: primaryStyles,
  secondary: secondaryStyles,
};
